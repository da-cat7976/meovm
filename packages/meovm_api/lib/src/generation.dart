/// Empty interface of view model used for code generation and lints.
abstract interface class MeovmAutoVm<Param extends MeovmAutoVmParameter?> {
  // Intentionally left blank
}

/// Empty interface of view model member used for code generation and lints.
abstract interface class MeovmAutoVmMember {
  // Intentionally left blank
}

/// Empty interface of view model parameter used for code generation and lints.
abstract interface class MeovmAutoVmParameter {
  // Intentionally left blank
}

/// Empty interface of view model owner used for code generation and lints.
abstract interface class MeovmAutoVmOwner<Param extends MeovmAutoVmParameter?> {
  // Intentionally left blank
}

/// Empty interface of view model feature used for code generation and lints.
abstract interface class MeovmAutoVmFeature {
  // Intentionally left blank
}
